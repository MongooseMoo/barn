Read-only bounded review; own only reports/fable-checkpoint-admission-20260918.md. Read AGENTS.md. Do not edit code, run conformance, or commit.

We found one full-suite failure beyond 79 also reproduced on baseline 83751cc: audit_waif_values_in_suspended_vm_survive_restart expected before="waif-marker" but got "resumed-marker" after restart. Canonical WSL Toast passes this unchanged test. server.checkpointWith currently captures runtime.TaskSnapshots BEFORE dbformat.WriteCheckpoint separately takes Store.SnapshotWithRoots' exclusive gate. A resumed task can therefore be captured, run and mutate its WAIF, then have the mutated store serialized with the older saved continuation, replaying it after restart. Source-audit this explanation and recommend the smallest correct fix that works at admission cap one. No speculation presented as fact.

Proposed fix being considered: admission.Controller.Pause(ctx) stops NEW grants, waits active reservations to settle, and returns an idempotent resume. Server checkpoint runs checkpoint_started BEFORE pausing and checkpoint_finished AFTER resuming. Runtime checkpoint callback holds SweepMu then VMStartMu under that pause; captures task snapshots plus the complete checkpoint writer under the barrier. Existing sweep hooks are exempt but excluded by SweepMu/VMStartMu; nested hooks borrow active scopes; completion callbacks have already released admission. Eval releases its scope during suspension but retains physical root lease, so do NOT wait for physical-lease count to reach zero: it would deadlock awaiting readmission. TaskSnapshots does not publish intrinsic Eval's in-progress VM today. Check whether a suspended Eval can still mutate state outside admission/sweep protection. Keeping disk I/O in the paused interval is a latency limitation, but existing mutable WAIF references make releasing before serialization unsafe; a frozen checkpoint payload API would be a separate improvement. Evaluate this carefully, including shutdown and panic checkpoints, rather than assuming it is sound.

Also review a small implemented follow-up: taskCompletion pins result.Val before finalization and retains it until OnComplete returns. Regression TestAdmissionCompletionRetainsResultRootsUntilCallbackReturns failed before and passes under race after the fix. Scope.Finish uses at least 1 ns elapsed for started slices because coarse Windows clocks can return zero, which otherwise incorrectly refunds completed work.

Write ACCEPT/REVISE for the concrete checkpoint approach, required corrections if any, and root handoff review. No broad architectural redesign; identify actual cycles/data races and minimum repair.
