Perform a bounded final source review of the shared admission implementation in C:/Users/Q/code/barn. Read AGENTS.md. Own only reports/fable-admission-final-20260918.md. Do not edit implementation, run tests, commit, or push. Other work is ongoing; do not revert changes.

Your previous implementation review accepted the architecture subject to F1/F2 fixes. Verify those exact fixes in the current tree: cancelled background admission no longer kills queued tasks on Runtime.Stop; runTaskAdmitted suppresses lifecycle context cancellation without sending raw errors to players; player user_* hooks now enter via RunServerVerbTaskWithArgstr while four server lifecycle hooks remain System. New tests cover cap-one suspension, callbacks, error hooks, kill/admission and kill/gate races, roots, input-stop/system-hook separation, Stop preserving queued continuations, service-based background ordering, configuration, and a 1024-line forced-input burst.

Other changes since your snapshot: background keys/order are computed once using a locked ledger snapshot (no changing comparator); phantom negative-ID lanes retire when empty; EnqueueInput/Stop serialize cancellation and complete accepted Done channels; ResumeBackground retains its group; old EscalationLock/Unlock/ExemptFromCommitGate tokenless APIs have been deleted, with tests migrated to explicit grants. Read the new plan implementation section and flag any inaccurate claim. Admission tests have passed under race; full Go and broad race runs passed; full managed cap 1/16 conformance is running. Do not treat those statements as your own verification.

Check only these changes and any concrete blocker they expose. Return ACCEPT or REVISE with concise evidence. Remaining documented scope limits (O(principals) watermark scan, unbounded forced-input mailbox, cooperative slices, pre-existing direct writes, deferred maintenance exemption, unmeasured Mongoose performance) are not new implementation requirements.
